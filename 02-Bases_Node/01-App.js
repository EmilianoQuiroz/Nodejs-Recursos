/**
 * Tabla del 5 por consola
 */

/**
 * Limpiar consola 
 */
console.clear();

/**
 * Mostrar por consola la tabla del 5
 */

// Base a multiplicar
let base = 5;

// Ciclo for para generar la tabla que se va a mostrar por pantalla
for(let i = 1; i <= 10; i++){
   
    console.log(`${base} x ${i} = ${base * i}`);

}